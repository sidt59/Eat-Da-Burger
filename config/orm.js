// import mysql
var connection = require("../config/connection.js");

    // functions to insert
    /* `selectAll()`
     * `insertOne()`
     * `updateOne()` */

// create var orm. make sql calls inside

// orm export
module.exports = orm;